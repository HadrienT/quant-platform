// Broker side of SASL/SCRAM: only names the login module. The credentials themselves
// live in the cluster metadata (created with kafka-configs --alter --add-config SCRAM-SHA-256).
KafkaServer {
    org.apache.kafka.common.security.scram.ScramLoginModule required;
};
